{
	"$schema": "https://turborepo.dev/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build", "^build:rust"]
		},
    "build:rust": {
      "cache": true,
      "outputs": ["*.node", "*.d.ts", "*.js"]
    },
    "test": {},
    "check": {},
    "machete": {}
	}
}
